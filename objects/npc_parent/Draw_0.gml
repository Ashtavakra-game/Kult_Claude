// Draw Event obiektu npc_parent

// najpierw rysujemy standardowo sprite
draw_self();
/*
// sprawdzamy, czy npc_data istnieje
if (!is_undefined(npc_data)) {
    var y_off = -16; // przesunięcie nad głową
    var x_off = 0;

    // przykładowe wartości do wyświetlenia
    var txt = "State: " + string(npc_data.state) + "\n" +
              "Target: " + string(npc_data.target) + "\n" +
              "Idle timer: " + string(npc_data.idle_timer);


    draw_set_color(c_white);
    draw_set_font(fnt_menu); // jeśli masz font do debugowania, lub użyj domyślnego
    draw_text(x + x_off, y + y_off, txt);
}
*/